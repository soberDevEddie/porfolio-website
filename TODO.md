# TODO

## Profile photos
- [ ] Take/select photo(s) of myself to use on the portfolio (headshot for About section, optional casual/action shot for Hero).
- [ ] Add photo files to `public/` (e.g. `public/headshot.jpg`).
- [ ] Update [About.tsx](src/components/About.tsx) to render the headshot next to the bio text.
- [ ] Consider swapping or supplementing the Chicago skyline background in [Hero.tsx](src/components/Hero.tsx) with a personal photo.
- [ ] Optimize images (compress/resize, add `width`/`height` and descriptive `alt` text) before committing.

## In progress
- [ ] Typeform integration — connect Gmail to Typeform for contact form entries (started in 3e302aa).
